<!DOCTYPE html>
<html>
<head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <title>Anagrams</title>
</head>
<body>
    <div class='container'>
        <h1>Anagrams</h1>

        {% if result is defined %}
          <h2>{{ result }}</h2>
        {% endif %}

        <form action='/result'>
            <div class='form-group'>
              <label for='phrase'>Enter the phrase:</label>
              <input id='phrase' name='phrase' class='form-control' type='text' required>
            </div>
            <div class='form-group'>
              <label for='find'>Enter the search word:</label>
              <input id='find' name='find' class='form-control' type='text' required>
            </div>
            <div class='form-group'>
              <label for='replace'>Enter the replacement word:</label>
              <input id='replace' name='replace' class='form-control' type='text' required>
            </div>
            <button type='submit' class='btn btn-success'>Submit</button>
        </form>
    </div>
</body>
</html>
