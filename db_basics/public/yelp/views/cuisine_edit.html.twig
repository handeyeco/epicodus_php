<!DOCTYPE html>
<html>
<head>

<title>Yelp</title>

</head>

<body>

<h1><a href="/">Yelp</a></h1>

<a href="/cuisine/{{ cuisine.id }}">Back to {{ cuisine.name }}</a>

<h2>Edit {{ cuisine.name }}</h2>
<form action="/cuisine/{{ cuisine.id }}" method="post">
  <input name="_method" type="hidden" value="patch" />

  <label for="name">Name:</label>
  <input id="name" name="name" type="text" value="{{ cuisine.name }}" required />

  <button type="submit">Change Cuisine</button>
</form>

<h2>Delete {{ cuisine.name }}</h2>
<form action="/cuisine/{{ cuisine.id }}" method="post">
  <input name="_method" type="hidden" value="delete" />
  <button type="submit">Delete Cuisine</button>
</form>

</body>
</html>
